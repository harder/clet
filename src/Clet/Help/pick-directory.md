## Examples

```sh
# Directory picker:
clet pick-directory

# Short alias:
clet dir

# Start in a specific directory:
clet dir --root ~/projects

# JSON output:
clet dir --json
# → {"schemaVersion":1,"status":"ok","value":"/Users/tig/projects/myapp"}
```
