## Examples

```sh
# Color picker:
clet color

# Pre-filled:
clet color --initial "#ff6600"

# With a title:
clet color --title "Choose accent color"

# JSON output (hex):
clet color --json
# → {"schemaVersion":1,"status":"ok","value":"#ff6600"}
```
